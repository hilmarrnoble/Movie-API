# Movie API

A Node.js API for managing movies and users, using MongoDB, JWT, and Google OAuth.

## Features
- CRUD for movies
- JWT + Google OAuth
- Swagger API Docs

## Setup

```bash
npm install
cp .env.example .env
npm start
